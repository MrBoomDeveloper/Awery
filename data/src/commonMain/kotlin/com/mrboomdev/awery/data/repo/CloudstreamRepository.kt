package com.mrboomdev.awery.data.repo

object CloudstreamRepository {
	fun parse(url: String, json: String): Repository {
		throw InvalidRepositoryException("Cloudstream repositories aren't done yet!")
	}
}